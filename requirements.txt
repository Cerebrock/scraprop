# HTTP + parsing
beautifulsoup4
lxml
requests
python-dotenv

# Stealth browser for MercadoLibre (anti-bot challenge).
# patchright is an undetected fork of playwright; install browser with:
#   patchright install chromium
patchright

# LLM scorer
google-genai
